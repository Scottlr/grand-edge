Start here:
1. Open `docs/corpus/router.md`.
2. Pick the route whose `helps_with` list matches the request.
3. Run the route's `search_first` commands before opening source files.
4. Read only the listed docs and at most `max_files_to_open` source files.
5. Report source ids, review status, and graph/corpus version context.
