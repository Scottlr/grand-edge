# Corpus Docs

GrandEdge corpus files are supporting evidence and implementation context, not
price truth. Planned relation and event source files live under
`data/relations/` and `data/corpus/`.

Open [router.md](router.md) first before reading any corpus source.
Use `rg` to locate relevant entries and sample only the smallest needed slice
instead of loading full JSON files.
