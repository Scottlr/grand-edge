# Journey QA

All recommendation surfaces are currently covered by the shared checklist in
`apps/web/src/content/recommendationSurfaces.ts`.

Skipped surfaces:

- None right now. If a future surface cannot satisfy the shared checklist, add
  the explicit reason to the matching `skipReason` field and document it here.
